import styled from '@emotion/styled';
import Home from './dom';

export const StyledHome = styled(Home)`
  > .wrapper {
    padding: 32px;
  }

  > h1 {
    margin-bottom: 24px;
    font-size: 32px;
    text-align: center;
  }
`;
